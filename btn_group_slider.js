/*
const btnTrack = document.querySelector(".btn_track");

for (let i = 0; i < MENU.length; i++) {

    let menuItem = document.createElement('button');

    if (MENU[i].order.toString() === '1') {
        menuItem.classList.add('orange_btn_start');
    }
    if (MENU[i].order.toString() === (MENU.length/2).toString()) {
        menuItem.classList.add('orange_btn_middle');
    }
    else {
        menuItem.classList.add('orange_btn');
    }

    let menuItemText = document.createTextNode(MENU[i].title.toString());
    menuItem.appendChild(menuItemText);

    btnTrack.appendChild(menuItem);
}


let positionBtn = 0;
const slidesToShowBtn = 4;
const slidesToScrollBtn = 1;
const track = document.querySelector(".btn_track");
const containerBtn = document.querySelector(".btn_container");
const item = document.querySelector(".orange_btn");

*/
